<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Погода</title>
    <link rel="stylesheet" href = "/css/styles.css">
</head>
<body>
    <div class="app">
        <div class="app__header">
            <% if(name) {%>
            <h1><%=name%></h1>
            <h2>Температура: <%=t%> С</h2>
            <h2>Влажность: <%=hum%> %</h2>
            <h2>Скорость ветра: <%=wi%> м/с</h2>
            <%}%>
            <% if(error) {%>
            <p class="error"><%=error%></p>
            <%}%>
        </div>
        <form action="/" method="POST" class="app__content">
            <div class="input">
                <input type="text" id="city" placeholder="Название города" name="city">
            </div>
            <button type="submit" class="btn">Показать погоду</button>
        </form>
    </div>
</body>
</html>